{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "dev": "ts-node-dev --respawn server.ts"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  },
  "dependencies": {
    "shared": "file:../shared"
  }
}
