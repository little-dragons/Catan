{
  "name": "server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "dotenvx run -f .env.development -- bun --hot Server.ts",
    "prod": "dotenvx run -f .env.production -- bun Server.ts",
    "migrate-dev": "dotenvx run -f .env.development -- bun database/MigrationsExecutor.ts && bun run generate-types-dev",
    "migrate-prod": "dotenvx run -f .env.production -- bun database/MigrationsExecutor.ts && bun run generate-types-prod",
    "remigrate-dev": "dotenvx run -f .env.development -- bun database/MigrationsExecutor.ts -remigrate && bun run generate-types-dev",
    "force-remigrate-prod": "dotenvx run -f .env.production -- bun database/MigrationsExecutor.ts -remigrate && bun run generate-types-prod",
    "generate-types-dev": "dotenvx run -f .env.development -- kysely-codegen --dialect sqlite --out-file database/DBTypes.d.ts",
    "generate-types-prod": "dotenvx run -f .env.production -- kysely-codegen --dialect sqlite --out-file database/DBTypes.d.ts"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/better-sqlite3": "^7.6.10",
    "@types/bun": "^1.3.10",
    "@types/uuid": "^9.0.8",
    "kysely-codegen": "^0.15.0"
  },
  "dependencies": {
    "@dotenvx/dotenvx": "^1.5.0",
    "@socket.io/admin-ui": "^0.5.1",
    "bcrypt": "^5.1.1",
    "kysely": "^0.27.3",
    "kysely-bun-sqlite": "^0.4.0",
    "shared": "1.0.0",
    "socket.io": "^4.8.3",
    "typescript": "^5.7.2",
    "uuid": "^9.0.1"
  }
}
