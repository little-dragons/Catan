import { beforeAll, describe, expect, it } from '@jest/globals'
import { Color, FullGameState, GamePhaseType, minimalGameState, Resource, robbableCrossingsForColor, TurnPhaseType } from 'shared'

describe('boardRules', () => {
    it('is empty', () => {
        expect(true).toBeTruthy()
    })
})