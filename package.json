{
  "name": "catan",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"cd server && bun run dev\" \"cd client && bun run dev\"",
    "prod": "concurrently \"cd server && bun run prod\" \"cd client && bun run prod\"",
    "test": "cd test/playwright && bun run test && cd ../logic && bun test"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  },
  "workspaces": {
    "packages": ["client", "shared", "server", "test/playwright", "test/logic"],
    "catalogs": {
        "frontend": {

        },
        "backend": {
            "kysely-codegen": "^0.20.0",
            "@socket.io/admin-ui": "^0.5.1",
            "better-sqlite3": "^12.6.2",
            "kysely": "^0.28.11",
            "kysely-bun-sqlite": "^0.4.0"
        },
        "shared": {
            "@types/bun": "^1.3.10",
            "@types/uuid": "^11.0.0",
            "@types/seedrandom": "^3.0.8",
            "@types/bcrypt": "^6.0.0",
            "socket.io": "^4.8.3",
            "socket.io-client": "^4.8.3",
            "bcrypt": "^6.0.0",
            "seedrandom": "^3.0.5",
            "structurajs": "^0.12.6",
            "typescript": "^5.9.3",
            "uuid": "^13.0.0"
        }
    }
  }
}
